<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xdd.busserverc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bus.BusOrderActivity"></activity>
        <activity android:name=".bus.BusListActivity" />
        <activity android:name=".bus.SearchBusActivity" />
        <activity android:name=".bus.AddBusActivity" />
        <activity android:name=".advice.AdviceListActivity" />
        <activity android:name=".advice.AddAdviceActivity" />
        <activity android:name=".AddAdminActivity" />
        <activity android:name=".AddTicketActivity" />
        <activity android:name=".ModifyPwdActivity" />
        <activity android:name=".ModifyInformationActivity" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".TicketDetailActivity" />
        <activity android:name=".ticketList.TicketListActivity" />
        <activity android:name=".BuyTicketActivity" />
        <activity
            android:name=".ContainerActivity"
            android:label="@string/title_activity_container" />
        <activity android:name=".FpwdActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>